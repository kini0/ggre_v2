Installation

~# apt-get update
~# apt-get install build-essential libssl-dev libcurl4-gnutls-dev libexpat1-dev gettext unzip -y
~# wget https://github.com/git/git/archive/v2.18.0.zip
~# unzip v2.18.0.zip
~# cd git-2.18.0
~# make prefix=/usr/local all
~# make prefix=/usr/local install
~# 
~# 

Mise a jour de git
~# git clone https://github.com/git/git.git
~# cd git
~# make prefix=/usr/local all
~# make prefix=/usr/local install

Configuration git

~# git config --global user.name "Sam Kooker"
~# git config --global user.email "ekooker@gmail.com"
~# git config --list
~# nano ~/.gitconfig


